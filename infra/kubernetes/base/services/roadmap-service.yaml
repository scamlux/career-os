apiVersion: apps/v1
kind: Deployment
metadata:
  name: roadmap-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: roadmap-service
  template:
    metadata:
      labels:
        app: roadmap-service
    spec:
      containers:
        - name: roadmap-service
          image: careeros/roadmap-service:latest
          ports:
            - containerPort: 8080
            - containerPort: 50051
          envFrom:
            - configMapRef:
                name: platform-config
          env:
            - name: SERVICE_NAME
              value: roadmap-service
            - name: GRPC_PORT
              value: "50051"
            - name: HTTP_PORT
              value: "8080"
            - name: DATABASE_URL
              value: postgres://postgres:postgres@roadmap-db.careeros.svc.cluster.local:5432/roadmap
---
apiVersion: v1
kind: Service
metadata:
  name: roadmap-service
spec:
  selector:
    app: roadmap-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 50051
      targetPort: 50051
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: roadmap-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: roadmap-service
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
