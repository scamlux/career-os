apiVersion: apps/v1
kind: Deployment
metadata:
  name: lms-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lms-service
  template:
    metadata:
      labels:
        app: lms-service
    spec:
      containers:
        - name: lms-service
          image: careeros/lms-service:latest
          ports:
            - containerPort: 8080
            - containerPort: 50051
          envFrom:
            - configMapRef:
                name: platform-config
          env:
            - name: SERVICE_NAME
              value: lms-service
            - name: GRPC_PORT
              value: "50051"
            - name: HTTP_PORT
              value: "8080"
            - name: DATABASE_URL
              value: postgres://postgres:postgres@lms-db.careeros.svc.cluster.local:5432/lms
---
apiVersion: v1
kind: Service
metadata:
  name: lms-service
spec:
  selector:
    app: lms-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 50051
      targetPort: 50051
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: lms-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: lms-service
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
